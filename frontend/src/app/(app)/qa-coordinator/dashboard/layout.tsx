/**
 * QA Coordinator dashboard content only. Global header/sidebar come from (app)/layout.
 */
export default function QaCoordinatorDashboardLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return <>{children}</>;
}
