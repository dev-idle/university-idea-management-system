/**
 * Staff content only. Global header/sidebar come from (app)/layout.
 */
export default function StaffLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return <>{children}</>;
}
