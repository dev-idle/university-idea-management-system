/**
 * User management area constants. Single source of truth for layout and copy.
 * Page size aligned with Role Manager list table standard (admin/constants).
 */
import { MANAGEMENT_PAGE_SIZE } from "../constants";

export const USERS_PAGE_SIZE = MANAGEMENT_PAGE_SIZE;

/** Number of table columns (Email, Full name, Role, Department, Status, Actions). */
export const USERS_TABLE_COLUMN_COUNT = 6;
